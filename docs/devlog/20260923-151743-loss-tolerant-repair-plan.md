# 20260923-151743 有损容忍与低成本备份修复方案

## 本轮目标和阶段

用户要求按“30%链路损失可以保留30%业务损失，但性能/无HOL/低延迟/突发稳定”更新开发方案，并要求所有性能测试一个Action run一条。本轮在next/tlslike-dataplane最新0fea99c基础上仅修改文档与状态，不修改正在开发的产品代码，不发起测试。

## 修改与原因

- 章程、AGENTS和正式计划入口统一新目标；WEAKNET_QUALIFICATION第10节为当前执行细则，第9节历史诊断保留。
- 第4节显式替换旧有损近零损失门槛：允许阶段p业务包损失，字节goodput另验目标×(1-p)×99%；无损0损失与99%吞吐、正确性和资源门不变。探针往返失败与单向损失分开；时延分位数不能过滤超时伪装通过。此为用户授权的loss-tolerant-v1，不得追溯改绿历史结果。
- 保持4096与3秒现有期限，优先改满窗淘汰的全量扫描；缓存备份失效不能被解释成业务包丢失。Seq/身份/所有权与FIN控制保护必须验证。用扫描操作计数和定向证据确认实际CPU贡献，不提前宣称解决雪崩。
- 所有性能run仅一个版本/配置/seed/场景。禁止同run A/B、顺序多样本及matrix；校准、微基准和soak同样适用。前后比较用独立run重复，诚实报告runner差异；普通unit/race独立执行。
- STATUS同步新任务，DECISIONS记录授权与取舍。现有功能36样本通过及后续race修复证据保留，不顺手重开已完成专项。

## 复用来源

无代码复用。依据已审计的当前runtimeowner/recovery.go evictRepairForFreshLocked、runtime.go发送路径及旧项目repair_horizon语义制定计划，未改old或REUSE_LEDGER。

## Actions证据

本轮文档修改没有运行Actions，产品行为、新分析器和单样本入口均NOT_RUN/待实现。此前run状态不因本次目标变更自动转PASS。仅检查文档差异、JSON格式与引用，不作为产品测试。

## 问题、排查与风险

FEC20:20不保证恢复，普通UDP不会自行重传；允许损失不是主动丢包配额，不允许本机drop/无效注入掩盖容量缺陷。窗口全满时的全扫描路径确实存在，但对实际塌陷贡献尚待测量。损失比例与字节吞吐不可混用；分片损失放大需报告，不能凭netem比例推断应用损失必然相同。

## 下一项原子任务

新agent从最新远端继续，先改造性能workflow为显式单样本并拒绝意外第二样本，再修淘汰索引及相关竞态/回绕/停流测试；分别启动Normal10M、Game4逻辑3M无损，再独立run弱网/突发验收。每轮留详细日志、源码/分析器SHA、run/job/artifact与未完成项，不因修改门槛宣称性能提升。
