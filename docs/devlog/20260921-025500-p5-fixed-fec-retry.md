# P5 fixed-FEC 20:10 candidate retry

- Date: 2026-09-21
- Branch authority: `next/tlslike-dataplane`
- Failed candidate SOURCE_SHA: `7aae4993e6bd78f3e75c732d98290586e7f55089`
- Failed candidate Actions: `35553888249`
- Previous product qualification remains: `24e6ff71d76b53d63719f83c5df8d2147a383977` / Actions `35529970723`
- Retry SOURCE_SHA: pending this commit
- Qualification: PENDING exact-SHA GitHub Actions

## First-candidate result

The fixed-FEC implementation itself reached and passed the dedicated P5 gate.

The exact-SHA harness emitted:

`WBD_P5_FIXED_FEC_CAPTURED source_sha=7aae4993e6bd78f3e75c732d98290586e7f55089 profile=20:10 flows=1 outer_connections=1 client_source=41 client_parity=21 server_source=245 server_parity=125 fec_recovery=0 repair=0 padding=off wire_amp=1.912429 records=432`

and the independent validator emitted the same fixed-FEC inventory with PASS.

The existing P5 certificate/full-resumed, sparse-single-connection, and natural-concurrency harnesses and independent validators also passed on the same SHA.

Combined P5 artifact:

- ID `10619382571`
- size `1052735` bytes
- digest `sha256:52e51294c1df59284227befb33e374fee0d0dae836f890394f542aee917bace9`.

## Workflow failure classification

Actions `35553888249` completed 7/8 jobs PASS.

The only failing job was Ubuntu active-go-tests. The failure was in the unchanged P4 lifecycle closure test:

`TestLifecycleEntryGameThreeAndFourLaneMatrix/lanes-4`

with server owner statistics showing:

- GameDelivered = 1
- GameDuplicates = 2

at the test assertion.

Repository contract, Windows active-go-tests, P2 kernel fallback, OpenWrt privileged runtime, Linux shared-TUN privileged iptables/nft, and the full P5 measurement gate all passed.

This retry does not modify P4, the FEC implementation, CLI policy, measurement harness, workflow, or validator. It records the failed evidence and creates a new exact candidate SHA for the complete workflow to rerun.

If the P4 lanes matrix failure repeats reliably on the retry, it must be investigated as a separate possible hard regression before P5 qualification. A single unrelated failure is not grounds to silently weaken the P4 gate.

## Qualification rule

The retry must pass all eight jobs at one exact SHA. The P5 fixed-FEC evidence must remain:

- explicit profile 20:10, not a default/recommendation;
- one real TLS1.3 full HTTPS flow;
- one reused outer connection;
- source and parity shards in both directions;
- zero pending partial blocks before snapshot;
- zero FEC reconstruction/recovered sources under no loss;
- zero injected network drop and capture loss;
- zero runtime repair retransmits;
- production padding off / zero padding bytes;
- independently recomputed steady outer / inner TLS wire amplification.

`last_tested_source_sha` remains `24e6ff71d76b53d63719f83c5df8d2147a383977` until the retry earns full workflow qualification.
