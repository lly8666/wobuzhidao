# P5 40 Mbps aggregate-inner candidate

- Date: 2026-09-21
- Branch: `next/tlslike-dataplane`
- Authority before write: `4ac3ffdf20938ccb72450c45aec2f2768072d3c5`
- Previous product load qualification: `d9e09053810f07db5e19c81a59970c831fa6e12a` / Actions `35573152055` / 20/20 PASS.
- Previous docs-only closure: `4ac3ffdf20938ccb72450c45aec2f2768072d3c5` / Actions `35573532133` / 20/20 PASS.

## Scope

This starts only the prescribed 40 Mbps aggregate-inner target. It does not add 50/100 Mbps exploration, weak-network load, >=30 minute soak, FEC tuning, or padding enablement.

## Reuse of qualified load contract

No new workload algorithm is introduced. The already-qualified load harness and validator are reused unchanged except that 40 Mbps is added to the allowed target set.

The workload remains:

- one persistent formal BusinessFlow on one Normal lane,
- real inner TLS 1.3 + HTTP/1.1 POST/echo,
- 1 KiB / 8 KiB / 32 KiB bidirectional payload cycle,
- target scope = C2S + S2C inner application payload,
- 15 second absolute-clock offered-load window,
- FEC off, padding off, no network injection,
- offered/accepted/actual, send failure, skipped slots, send lag, RTT, PPS, wire bytes, resource and queue evidence retained.

Two separate Actions VMs use seeds `20260933` and `20260934`.

## Qualification rule

The independent validator keeps the same threshold used for the qualified 5/10/15/20 Mbps atom: actual aggregate application payload must reach at least 95% of offered bytes, while application corruption/loss/duplicate and send failure remain zero.

If the runner cannot sustain the target, the validator emits `CAPACITY_LIMITED` or `INJECTION_UNDERRUN`. CPU/PPS/memory are observational only and are not cross-VM absolute gates.
