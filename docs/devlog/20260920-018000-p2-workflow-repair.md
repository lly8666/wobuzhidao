# 2026-09-20 P2 pcap drain workflow语法修复

## 基线与失败证据

基线 `d96d2fdadf229aa3112d11a6a3e6eaef237167fc`。GitHub Actions run 35458809874 在调度前直接FAIL，workflow run没有任何job记录。检查提交diff发现上轮自动文本替换截断了 `-test.run` 的引号，并把job后半段重复插入到shell块中；这是CI文件损坏，不是产品实现或网络资格失败。

上一可执行源码 `9679f4c65688a83fc8f57237880d651a08dfcda1` 的repository-contract、Windows/Linux unit/build、Linux race/fuzz均PASS，kernel Go测试1.03s PASS；其唯一缺口仍是tcpdump在立即SIGINT前未drain完整capture。

## 修改

从 `9679f4c` 的已成功解析workflow完整恢复 `.github/workflows/next-foundation.yml`，只在kernel测试完全结束、双方协议socket均已关闭后、`kill -INT tcpdump` 前加入固定1秒capture drain。静态校验workflow中只有一个kernel test命令和一个P2 artifact上传块。

不修改任何产品Go源码，不触碰P3 tlsrecord/pathmtu/FEC/LINK/datapath。

## Actions / SOURCE_SHA

本提交创建前未在本地运行任何测试。d96d2fd因workflow pre-dispatch失败不提供产品资格。本提交必须重新运行全部Actions并生成新的pcap/summary。

## 下一步

若本SHA完整PASS，核对新pcap artifact与SOURCE_SHA后更新唯一STATUS关闭P2；若失败继续按新的原始job证据修复。
