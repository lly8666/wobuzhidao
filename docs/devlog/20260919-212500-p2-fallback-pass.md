# 20260919-212500 P2 Real Decoy Fallback Actions回执

SOURCE_SHA：f9bb007f81da5cf772d83ff37d8086987ce91543

Run：https://github.com/lly8666/wobuzhidao/actions/runs/35445121111

顶层结果：completed / success。

通过：
- repository-contract
- Windows go list / go test ./... / go build ./...
- Ubuntu go list / go test ./... / go build ./...
- Ubuntu race
- 既有tlsrecord directed fuzz
- independent P1 reference generator
- artifact upload

本SHA最终证明：
- ServerAssociation单入口只消费一次ClientHello；
- recognized WBD分支进入protected admission且fallback dial count=0；
- unrecognized分支把同一Raw ClientHello逐字节replay到真实TLS decoy；
- decoy TLS证书/握手和应用pong经同一个FakeTCP association返回client；
- fallback分支不PrepareTransition；
- SNI mismatch在dial前拒绝；
- decoy TLS close_notify得到FakeTCP ACK，splice正常退出而非依赖5s SessionTimeout；
- Linux race覆盖该fallback/测试transport并发路径。

失败链：
- e98ee2cd：测试编译错误，产品未运行；
- b0d6f780：运行期暴露无half-close退出及test peer阻塞Read；
- 2abd1520：SNI退出已修，剩余close_notify未ACK；
- 44b13410：5s阻塞已消失，但测试先uTLS.Close导致底层提前关闭；
- f9bb007f：按真实TLS shutdown顺序消费close_notify并ACK，全套PASS。

P2尚不宣布完成。下一步按ACCEPTANCE逐项审计并补齐：
- 最后bootstrap丢包/重传/ACK丢失
- 首条新record丢失/提前到达
- TLS reader预读隔离
- 退出候选
- transition队列上限
- record no-HOL核心资格
- 抓包格式门槛（若当前测试边界不能证明则明确保留为未验证）
