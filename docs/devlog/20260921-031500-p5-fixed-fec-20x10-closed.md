# P5 fixed-FEC 20:10 atom closed

- Date: 2026-09-21
- Branch authority: `next/tlslike-dataplane`
- Product SOURCE_SHA: `b7c0b3d334ba1c52f9f53479eb8a2f4e4de692e3`
- GitHub Actions: `35555303253`
- Result: **8/8 PASS**
- Previous failed candidate: `7aae4993e6bd78f3e75c732d98290586e7f55089` / Actions `35553888249`
- This closure commit is docs-only and must not replace the product qualification SHA.

## Closed atom

The seventh P5 atom is closed: explicit fixed-FEC capability/cost measurement under a controlled lossless real-HTTPS workload.

The active product entry had no non-zero FEC selection before this atom. The formal client/server commands now expose exactly one configuration knob:

`--fec-parity`

with default `0` and accepted values only:

`0,4,8,10,12,16,20`.

Non-zero selection uses the existing active-tree runtime bounds:

- partial block flush: 8 ms;
- decoder heavy block bound: 8.

No adaptive profile, flush tuning, block-bound tuning or profile competition is added.

Production default remains FEC off.

## Runtime wiring

The atom closes a formal-runtime wiring gap discovered during P5 audit.

The lane FEC path already implemented `FlushDue`, but runtime tick did not drive partial-block parity emission. The qualified runtime now:

- ticks only the authoritative lane for newly formed FEC parity;
- calls the existing lane partial-block `FlushDue`;
- generation-fences returned records;
- sends due parity through the existing runtime transport;
- keeps retiring generations on repair tick only, so they cannot form new parity;
- drives RX LINK/FEC expiry from the same lane tick.

This changes no coding geometry, FEC wire format, repair RTO/horizon, or recovery algorithm.

## Read-only FEC inventory

The qualified stats path adds exact counters for:

Encoder:
- source shards emitted;
- parity shards emitted;
- full blocks;
- partial blocks;
- pending source count.

Decoder:
- actual Reed-Solomon reconstruction events;
- recovered source count.

A systematic-only complete block does not count as FEC recovery.

## Qualified controlled scenario

The P5 gate explicitly selects one existing fixed profile:

`20:10`

only as a controlled capability/cost scenario. It is not a recommendation or production default.

The exact-SHA lossless run uses:

- one real inner TLS 1.3 full HTTPS connection;
- one BusinessFlow;
- one existing outer Normal lane;
- controlled verified Root -> Intermediate -> leaf certificate chain;
- 256 KiB response payload;
- no network drop injection;
- capture loss 0;
- padding off;
- runtime repair enabled but required to remain unused;
- condition-driven wait for client/server encoder pending-source counts to settle at zero.

Exact marker:

`WBD_P5_FIXED_FEC_CAPTURED source_sha=b7c0b3d334ba1c52f9f53479eb8a2f4e4de692e3 profile=20:10 flows=1 outer_connections=1 client_source=42 client_parity=22 server_source=246 server_parity=126 fec_recovery=0 repair=0 padding=off wire_amp=1.930542 records=436`

The independent validator recomputed and accepted the same inventory.

## Cost accounting

The scenario records numerator and denominator separately and reports:

`steady outer IPv4/TCP wire bytes / inner TLS ciphertext bytes`

as wire amplification.

For the qualifying run the ratio was:

`1.930542`.

This ratio includes the actual fixed-FEC and outer transport/record cost in the measured steady interval. It is cost evidence only, not a performance target or claim that 20:10 is desirable.

Capture loss, injected network drop, FEC reconstruction/recovery, runtime repair and padding remain separate counters. In this lossless run all four loss/recovery-side counters are zero while parity emission is non-zero.

## Retry history

The first candidate `7aae4993…` passed the full P5 gate but the overall workflow failed only in an unchanged P4 lanes-4 lifecycle unit assertion. That failure was preserved in STATUS.

The retry SHA made no product-code change relative to that candidate. On `b7c0b3d3…`, the P4 matrix and Linux race both passed, and all eight jobs completed successfully.

## Artifacts

Combined P5 artifact:

- ID `10620018135`
- size `1057166` bytes
- digest `sha256:74e3575ef202b8f7267de86148bff765e39834dba2873a17800b6e327bd59d8f`.

It contains the fixed-FEC raw PCAP/events/manifest/summary/test log together with the previously qualified P5 measurement sets.

## Non-claims

- 20:10 is not a production default, preferred profile or optimum.
- Weak-network recovery is not yet qualified.
- Load and soak are not yet qualified.
- Padding remains production `0/off`.
- No classifier or indistinguishability conclusion.
- Windows/Npcap physical remains P7 `NOT_RUN`.
- OpenWrt IPv6 remains `NOT_IMPLEMENTED`.
- Existing runtimeowner cumulative ACK / 4096 metadata / 1s default RTO / 3s repair horizon remains unchanged.
- No archive implementation was reused; `docs/REUSE_LEDGER.json` is unchanged.

## Next atom

Implement only the first prescribed weak-network profile from `docs/ACCEPTANCE.md`:

- 300 ms one-way delay;
- total 120 seconds;
- 30 / 60 / 30 second phases;
- network drop 5% -> 20% -> 5%.

The harness must report actual injection/drop location separately from capture loss, FEC, repair and padding, and preserve raw data for goodput, application loss/duplicate, RTT p50/p95/p99, post-5% recovery, CPU/PPS/memory/GC, queue residency and wire amplification.

Do not mix the 5% -> 30% -> 5% profile, load matrix, 30-minute soak, FEC profile tuning, or padding enablement into this atom.
