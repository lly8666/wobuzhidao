# 20260919 新分支隔离与单数据面开发基线

## 本轮目标和阶段

P0。按用户授权在同仓库建立 `next/tlslike-dataplane`，从DTLS预发布的b5c848f开始；不实现产品代码，不跑本地测试。用户明确不兼容旧架构、不做算法或新旧性能对比，所有开发测试在Actions，物理机留最终阶段。

## 修改与原因

完整旧树移至old，原README改名README.legacy.md；旧.github workflows移入old后不再活跃。新根写AGENTS/章程/计划/协议/模块地图/路线/验收/唯一STATUS/日志模板。

当前旧CONTINUE_HERE仍指向早期分支与包，且旧文件对FEC与物理lane余量描述不同；整树隔离防止这些提示成为新任务来源。新章程明确1..4逻辑lane与最新6退休/10物理上限，保留真实TLS建连、有限恢复、FEC、Game、DORMANT和平台网络清理。

决定唯一ChaCha20-Poly1305+完整PN保护record，新建单进程运行时；复用算法但不导入DTLS进程或兼容模式。没有算法选型或旧项目A/B工作项。

## 复用来源

仅归档原树，尚无任何代码提取。来源b5c848f4e9afdffd15d1bc451560edf4e9390a35。MODULE_MAP逐项界定后续提取范围，REUSE_LEDGER为空。

## Actions证据

首次提交发布前NOT_RUN。新增foundation检查仅验证文档/交接/归档和条件Go基础测试；产品尚未实现，不能称ACTIONS_PASS产品。发布后在STATUS与后续日志记录精确run和SOURCE_SHA。

## 问题、排查与风险

old源码可供参考但旧提示不具权威；不允许运行依赖old。新的record wire是自定义格式，必须做独立向量和乱序/损坏测试。有限ACK语义与标准TCP存在已知差异，不能承诺所有抓包无提示。P0绿灯不代表端到端实现。

## 下一项原子任务

P1：创建根Go module（Go1.23.12工具链），实现tlsrecord方向密钥与record encode/decode固定向量；推送后用Actions Linux/Windows unit、Linux race/fuzz验证。不要先迁移整个old/internal，不修改FEC或recovery参数。
