# 2026-09-20 P2 pcap capture drain同步

## 基线与失败证据

基线 `9679f4c65688a83fc8f57237880d651a08dfcda1`，run 35458665989：repository-contract、Windows/Linux unit/build、Linux race/fuzz全部PASS；privileged kernel Go测试也PASS并从前一轮60.24s降到1.03s，显式 `server.Close<=1s` 成立。

job失败发生在测试完成后的pcap分析：tcpdump打印 `13 packets captured / 56 packets received by filter / 0 packets dropped by kernel`。因此不是网络丢包或capture loss，而是workflow在测试返回后立即SIGINT，libpcap尚未从kernel packet socket消费已排队的后半段流；分析器自然看不到先前已由emitter确认发生的>=1s重传。失败artifact 10588991467，zip SHA256 `40cc38c14e07fefe277783f828c593fa38742698da88f1c323c065a066b4d0c1`。

## 修改

只改Actions capture生命周期：内核/TLS/HTTP测试完全返回、双方TCP已关闭后，给tcpdump固定1秒post-test drain窗口，再发送SIGINT。该等待发生在协议和socket全部结束之后，仅让capture进程读取kernel中已经存在的包；不参与产品发包、ticket、TLS、fallback、重传节奏，也不是外观随机延迟。

产品源码、faketcp、realityfront、P3 tlsrecord/pathmtu/FEC/LINK均不修改。

## Actions / SOURCE_SHA

本提交前不在本地运行测试。上一SHA证明raw endpoint关闭有界，但pcap门FAIL；本提交必须重新产生完整pcap和summary，不能继承2d4be948旧pcap作为新源码资格。

## 下一步

本SHA若全部Actions PASS，核对pcap artifact与SOURCE_SHA后更新唯一STATUS关闭P2；否则继续按新的capture/协议原始证据修复。
