# P5 weaknet 5%->30%->5% candidate

- Date: 2026-09-21
- Branch: `next/tlslike-dataplane`
- Authority before write: `32f5d0afef74ebf779ffba7c00df98877e0d6c52`
- Previous closed product qualification: `d445e1634d6b48509bf2f44c0488a379fb75baf2` / Actions `35569185348` / 10/10 PASS.
- Previous docs-only closure regression: `32f5d0afef74ebf779ffba7c00df98877e0d6c52` / Actions `35569503395` / 10/10 PASS.

## Scope

This starts only the prescribed ninth P5 atom:

`300ms one-way / 120s / 30-60-30s / 5% -> 30% -> 5%`.

It does not start load, >=30 minute soak, FEC profile tuning, padding enablement, or a production policy selection.

## Implementation

- Preserve the qualified 5%->20%->5% scenario and validator.
- Parameterize the weak-network helpers by an explicit phase list so a dedicated `high30` phase cannot be mislabeled as `high20`.
- Add `high30` with 30% deterministic bidirectional loss for 30s..90s and its own deterministic phase salt.
- Use independent seeds `20260923` and `20260924`.
- Keep 300ms fixed one-way delay, 120s total duration, 30/60/30 phases, one HTTPS request every 3s, the existing 1KiB/8KiB/32KiB response cycle, FEC off, and padding off.
- Add an independent `check_p5_weaknet_5305_measurement.py` validator derived from the already-qualified raw-evidence rules, with 5/30/5 provenance and independent seed/salt constants.
- Add a separate `p5-weaknet-5-30-5` Actions matrix. Each matrix entry is a separate Ubuntu VM and runs exactly one seed/scenario/artifact. CPU/PPS/memory remain observational only.

## Qualification contract

The candidate must retain raw PCAP/event equality, deterministic loss provenance, minimum 300ms delivered-packet residency, application loss/duplicate accounting, RTT samples, final-5% third-consecutive-success recovery, FEC-off/padding-off evidence, repair/cost inventory, and raw resource evidence.

No 30% qualification is claimed by this commit itself. Exact-SHA Actions evidence is required before closure.
