# 20260919-192300 P2 REUSE_LEDGER契约修复

## 失败证据

SOURCE_SHA cb6000400ce8fa5c90af68a8ee4910e8ab5fbbc4 的 run 35437027533 在repository-contract失败，Go jobs未启动。

check_repository.py原始错误：Missing reuse source。

日志同时显示checkout SHA正确，archive验证已执行，因此不是runner或checkout异常。

## 根因

REUSE_LEDGER每个entry的source必须以old/开头并且是仓库中实际存在的单一文件。bootstrap_sender条目为了说明审查了arq.go与bootstrap_retransmit_test.go，误把source写成：

old/internal/faketcp/arq.go + old/internal/faketcp/bootstrap_retransmit_test.go

这不是合法路径，因此被契约正确拒绝。

## 修复

只修文档元数据，不修改产品代码：
- source恢复为真实代码来源old/internal/faketcp/arq.go。
- bootstrap_retransmit_test.go作为行为审查证据写入behavior_changes/tests说明，不伪装成单一source路径。
- STATUS登记本次FAIL，并明确Go测试尚未运行。

## 下一项

以修复后的精确SOURCE_SHA重跑Actions。只有repository-contract、Windows/Linux unit/build与Linux race实际PASS后，才能把Sender/transition子任务标记完成。
