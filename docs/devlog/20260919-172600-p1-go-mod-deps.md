# 20260919-172600 P1 Go module依赖修复

## 本轮目标和阶段

继续P1，不扩展范围。输入SOURCE_SHA为de284220bef3880ef126a67e3cb7ef43e02a98fe；只根据该SHA的Actions原始失败日志修复根go.mod依赖声明。

## 修改与原因

Actions run 35434610338 的Ubuntu job在Go 1.23.12上执行go list ./...时下载golang.org/x/crypto v0.38.0与golang.org/x/sys v0.33.0，随后明确失败：“go: updates to go.mod needed; to update it: go mod tidy”。

不在开发机运行tidy。根据DEVELOPMENT_PLAN与旧源快照已固定的版本，显式把golang.org/x/sys v0.33.0作为indirect require写入根go.mod；go.sum中对应v0.33.0校验和已来自old/go.sum的定向读取并在上一提交加入。无其他代码改动。

## 复用来源

无产品代码复用。依赖版本来源仍为old/go.mod与old/go.sum的b5c848f快照，只用于保持已决定版本；运行时不依赖old。

## Actions证据

- SOURCE_SHA：de284220bef3880ef126a67e3cb7ef43e02a98fe
- Run：https://github.com/lly8666/wobuzhidao/actions/runs/35434610338
- repository-contract：PASS
- Ubuntu active-go-tests：FAIL
- 失败点：go list ./...，go.mod需要补依赖元数据
- race/fuzz/reference vector：因前置失败均SKIPPED
- Windows在修复发起时仍未形成可替代的成功证据

因此de284220不得标记P1 PASS。

## 问题、排查与风险

这是第一次有明确证据的失败，修复严格对应日志，不做无关改动。修复后仍需重新检查编译、unit、race、fuzz和独立reference generator；任何后续失败按新日志处理。

## 下一项原子任务

等待本修复提交Actions。若基础Go步骤通过，读取Linux生成的tlsrecord-reference artifact并把固定keys/wire bytes固化到测试；随后再次以精确SOURCE_SHA完成P1验收。
