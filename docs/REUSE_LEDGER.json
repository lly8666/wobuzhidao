{
  "source_sha": "b5c848f4e9afdffd15d1bc451560edf4e9390a35",
  "entries": [
    {
      "source": "old/internal/faketcp/bootstrap_stream.go",
      "destination": "internal/faketcp/bootstrap_stream.go",
      "behavior_changes": "Preserve bounded ordered bootstrap buffering, ACK-gated chunked writes, deadlines, close semantics, and synchronous bootstrap payload tagging. Add a locked NextSeq accessor so the association can freeze the exact receive ownership boundary for prepare/detach; no payload-byte mode guessing is introduced.",
      "tests": "internal/faketcp/bootstrap_stream_test.go covers reordering, per-chunk ACK gating, deadlines, chunk/byte bounds, uint32 sequence wrap, marker scope, and close/EOF; association tests use NextSeq as the transition boundary."
    },
    {
      "source": "old/internal/faketcp/arq.go",
      "destination": "internal/faketcp/sequence.go",
      "behavior_changes": "Extract wrap-aware seqLT/seqLE only. No Receiver, SACK/RACK, repair pressure, or steady-state delivery policy is migrated here.",
      "tests": "bootstrap and transition tests exercise comparisons across uint32 wrap."
    },
    {
      "source": "old/internal/faketcp/arq.go",
      "destination": "internal/faketcp/bootstrap_sender.go",
      "behavior_changes": "Extract only copied Pending payload ownership, cumulative ACK release, ACK waiting, oldest-payload RTO retransmit, bootstrap 2s retransmit ceiling, and the invariant that bootstrap retries do not back off the shared RTO. The archived bootstrap_retransmit_test.go was also inspected as behavioral evidence. Steady-state SACK/RACK/repair-budget/partial-reliability logic is intentionally not migrated.",
      "tests": "internal/faketcp/bootstrap_sender_test.go covers the archived bootstrap ceiling invariant, later ordinary RTO backoff, cumulative ACK wait/deadline, retransmit Seq/payload identity, and uint32 wrap."
    },
    {
      "source": "old/internal/faketcp/packet.go",
      "destination": "internal/faketcp/packet.go",
      "behavior_changes": "Extract IPv4/TCP handshake/bootstrap build+parse, checksum and SYN option handling. WBD fixed MSS/SACK/window-scale remains a client presentation helper only; server initial-SYN validity is independent of that fingerprint. MarshalSegment now serializes the Segment's negotiated SYN options so ordinary peers can omit SACK/WS or advertise different values. Steady-state SACK blocks remain deferred.",
      "tests": "packet_test.go keeps fixed WBD/Windows persona checks; syn_compat_test.go covers ordinary legal SYN combinations, negotiated SYN-ACK option serialization and malformed initial SYN rejection."
    },
    {
      "source": "old/internal/faketcp/tcp_persona.go",
      "destination": "internal/faketcp/tcp_persona.go",
      "behavior_changes": "Preserve presentation-only legacy/windows11 persona selection while moving the actual SYN rewrite into the extracted packet builder. No sequence/reliability behavior depends on persona.",
      "tests": "internal/faketcp/packet_test.go fixes the Windows TTL/DF/SYN option ordering/checksum shape and proves the explicit legacy persona remains byte-identical to the base builder."
    },
    {
      "source": "old/internal/faketcp/tcp_persona_default_other.go",
      "destination": "internal/faketcp/tcp_persona_default_other.go",
      "behavior_changes": "Preserve non-Windows default legacy persona.",
      "tests": "Cross-platform Actions compile and run packet tests; explicit legacy persona wire identity is covered in packet_test.go."
    },
    {
      "source": "old/internal/faketcp/tcp_persona_default_windows.go",
      "destination": "internal/faketcp/tcp_persona_default_windows.go",
      "behavior_changes": "Preserve Windows default windows11 packet persona.",
      "tests": "Windows Actions compile/run the package; packet_test.go explicitly validates windows11 SYN presentation."
    },
    {
      "source": "old/internal/faketcp/server_mux.go",
      "destination": "internal/faketcp/association.go",
      "behavior_changes": "Extract ServerFlow, bounded association table, SYN/SYN-ACK/final-ACK, shared sequence space, bootstrap sender and transition routing. Audit closure separates legal TCP admission from WBD identity: any valid initial SYN is accepted, peer MSS/WS/SACK are recorded, missing IPv4 MSS defaults to 536, bootstrap chunking is capped by peer MSS, and SYN-ACK WS/SACK are offered only when negotiated. Steady-state Receiver/SACK/RACK/FEC remain deferred.",
      "tests": "internal/faketcp/association_test.go covers WBD SYN admission, cross-flow isolation, data-bearing final ACK delivery, real BootstrapStream Write -> Sender WaitAck -> incoming ACK unblock, exact transition boundary, early-record queue/detach, pure ACK ownership, table duplicate/cap behavior, same-association final-bootstrap payload loss retransmit with identical Seq/payload, ACK-loss retransmit without duplicate peer delivery, and a dropped first new-mode record whose successor still routes immediately; the successor is opened by tlsrecord at PN=1 to prove no HOL at the handoff. syn_compat_test.go additionally covers ordinary MSS/WS/SACK/no-option profiles and peer-MSS bootstrap sizing."
    },
    {
      "source": "old/internal/realityfront/front.go",
      "destination": "internal/realityfront/hello.go",
      "behavior_changes": "Extract the REALITY-like HMAC SessionID route marker, exact ClientHello identity parsing, replay connection and name normalization only. Ticket/auth/fallback dialing code is not migrated in this atomic task.",
      "tests": "internal/realityfront/tls_test.go validates the route marker on the pinned Firefox120 ClientHello and recognized/unrecognized classification behavior."
    },
    {
      "source": "old/internal/realitymirror/mirror.go",
      "destination": "internal/realityfront/hello.go",
      "behavior_changes": "Extract only bounded TLS ClientHello record reading plus SNI/ALPN parsing so classification can retain byte-exact raw hello for later fallback or replay. Target dialing, bidirectional splice and transfer limits remain deferred.",
      "tests": "internal/realityfront/tls_test.go verifies an unrecognized hello retains raw bytes and SNI for later fallback rather than being consumed irreversibly."
    },
    {
      "source": "old/internal/realityfront/single_flow.go",
      "destination": "internal/realityfront/tls.go",
      "behavior_changes": "Preserve the pinned uTLS Firefox120 ClientHello persona and same-connection crypto/tls server takeover, but remove username/password/ticket coupling from this atomic layer. Exporter keys are derived directly from the original uTLS/crypto-tls ConnectionState using WIRE_SPEC context; the old hand-copy into tls.ConnectionState is deliberately not reused. uTLS v1.6.5 Firefox120 couples renegotiation_info to RenegotiateOnceAsClient, which disables exporter access, so the extracted client keeps the renegotiation_info extension bytes/order but changes only its internal acceptance policy to RenegotiateNever before the final BuildHandshakeState.",
      "tests": "internal/realityfront/tls_test.go compares Firefox120 preset fields and performs real TLS 1.3 over ServerAssociation.BootstrapConn, requiring client/server exporter-derived tlsrecord KeyPair equality, context binding, and early-record prepare/detach preservation. internal/realityfront/renegotiation_test.go additionally proves the Firefox120 renegotiation_info extension stays byte-identical while its internal policy is RenegotiateNever."
    },
    {
      "source": "old/internal/realityfront/simple_auth.go",
      "destination": "internal/realityfront/admission.go",
      "behavior_changes": "Extract only one protected TLS application request carrying username/password, bounded length parsing, and constant-time credential comparison. Replace archived ticket issuance with the new P2 admission response required by WIRE_SPEC: record_version=1, exact 16-byte TunnelID, client/server record wire limits, and a server-generated 16-byte incarnation nonce. Unknown versions are explicitly rejected. No ticket filesystem, lease provider, installation ID, CLI, or fallback dialing code is migrated. Audit closure adds one absolute candidate establishment deadline spanning TLS plus protected admission through final-reply ACK/detach; context cancellation wakes blocked I/O, failure closes the candidate association/connection, and only successful handoff clears the deadline.",
      "tests": "internal/realityfront/admission_test.go covers successful protected admission over the real uTLS/crypto-tls FakeTCP association, server-generated nonce and parameter echo, exporter key equality from negotiated context, prepare-before-final-reply ordering, detached transition state, authentication failure without prepare, unknown-version rejection, TunnelID/context mismatch rejection, and record-limit bounds. audit_closure_test.go covers post-TLS silence, partial admission, context cancellation, unacknowledged final reply abort, and deadline clearing after success."
    },
    {
      "source": "old/internal/realitymirror/handle_from_hello.go",
      "destination": "internal/realityfront/fallback.go",
      "behavior_changes": "Extract only the post-classification decoy continuation: validate the already-parsed SNI, dial one fixed target, replay the exact already-consumed raw ClientHello before any splice bytes, then bidirectionally copy on the same inbound FakeTCP BootstrapConn with bounded optional per-direction bytes and session deadline. The single HandleServerAssociation consumes one ClientHello once: recognized WBD enters protected admission and never dials fallback; unrecognized enters decoy fallback and never prepares the record transition. Splice shutdown attempts TCP half-close when available and otherwise full Close; close-race errors io.ErrClosedPipe, net.ErrClosed, faketcp.ErrBootstrapClosed, ECONNRESET and EPIPE are treated as benign termination. Observer/demo witness logic is not migrated. HandleServerAssociation now applies the same absolute WBD candidate deadline during ClientHello classification and recognized admission; once classified as an ordinary visitor, that candidate deadline is cleared and fallback's own bounded session policy takes over.",
      "tests": "internal/realityfront/fallback_test.go covers byte-identical raw ClientHello replay into a real TLS decoy, application response on the same client association, final decoy TLS close_notify being drained/ACKed by the in-memory FakeTCP peer after application Close (matching kernel TCP ACK behavior), bounded splice termination, no transition prepare on fallback, recognized WBD with zero fallback dials, and SNI mismatch rejection before dialing. audit_closure_test.go proves non-WBD ordinary SYN profiles reach fallback classification without using the WBD SYN fingerprint."
    },
    {
      "source": "old/internal/linkdata/datagram_fragment.go",
      "destination": "internal/linkdata/datagram_fragment.go",
      "behavior_changes": "Archive source SHA b5c848f4e9afdffd15d1bc451560edf4e9390a35. Preserve WBDLFRG1 20-byte LINK fragment wire, non-zero uint32 PacketID identity, 65535-byte original datagram limit, out-of-order completion, identical-duplicate idempotence, conflicting duplicate/metadata rejection, 16 incomplete assemblies, 5s assembly TTL, 64 retired IDs and 10s retired TTL. Remove the archived dependency on internal/fec error sentinels and all Path/FEC runtime coupling. Reassembly storage is sparse instead of allocating declared fragment_count up front; add explicit buffered fragment/payload bounds and an owner-callable Expire method so idle traffic can still retire incomplete datagrams.",
      "tests": "internal/linkdata/datagram_fragment_test.go fixes exact wire fields, PacketID increment/wrap and reserved-magic escape; covers normal/out-of-order reassembly, identical duplicates, conflicting duplicates without replacing first arrival, explicit idle expiry, max fragment-count metadata without preallocation, entry/byte/assembly/retired bounds, malformed/oversize isolation, and the P3 no-HOL case where datagram A permanently misses one fragment while later complete datagram B is delivered first."
    }
  ],
  "note": "P2 audit closure remains qualified at product SOURCE_SHA 079a11a51a8afb7e977f5140d8cdcd21db1c81c5. P3 LINK fragmentation/reassembly extraction from archive source b5c848f4e9afdffd15d1bc451560edf4e9390a35 is qualified at SOURCE_SHA 310cdf95a2da601f6a6b4398ce9b8b178aa6a48a by Actions run 35451984199. FEC systematic fast-path, unified MTU, session and platform integration remain deferred."
}
