name: next-performance-ab-game-retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Explain replacement
        run: |
          echo "Retired by the one-workflow-run/one-sample contract."
          echo "Dispatch next-strict-weaknet separately for every source/config/seed/scenario."
          echo "Before/after and repeated measurements must be distinct workflow runs."
