name: next-performance-recovery-retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Explain replacement
        run: |
          echo "Retired because it mixed correctness tests and a performance sample."
          echo "Use foundation/targeted workflows for correctness and next-strict-weaknet for one measurement."
